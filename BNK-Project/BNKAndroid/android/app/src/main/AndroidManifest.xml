<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bnkandroid">

    <!-- 네이버 지도 / 네트워크 / 위치 권한 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:label="BNKAndroid"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
        <!-- ↑ 개발 중 http://192.168.x.x 로컬 서버 통신 허용 (필요 없으면 삭제해도 됨) -->

        <activity
            android:name="com.example.bnkandroid.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!-- Flutter 엔진 초기화 중에도 launch_background를 유지(파란 로고 방지) -->
            <meta-data
                android:name="io.flutter.embedding.android.SplashScreenDrawable"
                android:resource="@drawable/launch_background" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 네이버 지도 API 키 -->
        <meta-data
            android:name="com.naver.maps.map.CLIENT_ID"
            android:value="zsmkzlyhar" />

        <meta-data
            android:name="com.naver.maps.map.NCP_KEY_ID"
            android:value="zsmkzlyhar" />

        <!-- Flutter 임베딩 v2 사용 -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
